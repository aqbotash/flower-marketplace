{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "dockerfile",
        "watchPatterns": [
            "/app/*.py",
            "/app/**/*.py",
            "/Dockerfile",
            "/pyproject.toml"
        ]
    },
    "deploy": {
        "restartPolicyType": "ON_FAILURE"
    }
}
